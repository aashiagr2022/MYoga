<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.netra.myoga">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.GET_ACCOUNTS"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Myoga">
        <activity android:name=".LogoPageActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".LoginActivity" />
        <!--<activity android:name=".UserProfileActivity" />-->
        <activity android:name=".UserDetailsActivity" />
        <activity android:name=".UserPurposeActivity" />
        <activity android:name=".TransitionPageActivity" />
        <activity android:name=".Level1Activity" /> <!--android:theme="@style/Theme.AppCompat.Light.NoActionBar"-->
        <activity android:name=".Level2Activity" />
        <activity android:name=".Level3Activity" />
        <activity android:name=".Level4Activity" />
        <activity android:name=".PoseCount"/>
        <activity android:name="PoseControlFlow"/>
        <activity android:name=".CompletionActivity"/>

        <!--meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" /-->
    </application>

</manifest>